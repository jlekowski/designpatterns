formatter.name: pretty

suites:
    strategy_suite:
        namespace: DesignPatterns\Strategy\
        psr4_prefix: DesignPatterns\Strategy\
        src_path: Strategy/src
        spec_path: Strategy

    factory_suite:
        namespace: DesignPatterns\Factory\
        psr4_prefix: DesignPatterns\Factory\
        src_path: Factory/src
        spec_path: Factory

    dataMapper_suite:
        namespace: DesignPatterns\DataMapper\
        psr4_prefix: DesignPatterns\DataMapper\
        src_path: DataMapper/src
        spec_path: DataMapper

    observer_suite:
        namespace: DesignPatterns\Observer\
        psr4_prefix: DesignPatterns\Observer\
        src_path: Observer/src
        spec_path: Observer
